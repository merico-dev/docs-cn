# 版本更新说明 v3.196.1

| 版本号<br/>   | 更新日期<br/>   | 备注<br/>         |
| ------------- | --------------- | ----------------- |
| v3.196.1<br/> | 2026-02-02<br/> | 迭代版本更新<br/> |

## 功能优化

- EE-19898 按照新的设计来优化之前的 AI 编程看板
- EE-19961 优化看板下钻提交列表的权限校验逻辑，解决部分权限用户无法查看明细列表的问题
- EE-20016 代码库分析列表 - 支持按授权信息筛选
- EE-20077 OpenAPI 代码库列表相关接口增加 creator 相关字段
- EE-20093 账号导出功能新增创建时间字段

## 新增功能

- EE-19730 AI 图表解读支持对话模式，允许用户通过追问来了解更多与指标相关的信息
- EE-20017 OAuth 2.0 类型的单点登录功能，支持通过环境变量的代理配置
- EE-20018 支持开发者在“个人设置”页维护自己账号的邮箱信息

## 修复

- EE-20015 研发效能团队摸底分析-组织效率-人均生产率均值更正

## OpenAPI DIFF

新增接口 `/v2/app/logs/login/list` 查询登录历史记录，支持按用户名、时间范围、角色、登录类型等条件过滤， 本方法需要登录账户拥有至少以下一个 workspace 的权限: [toolbox -> auth-log -> login-history]

新增接口 `/v2/app/logs/operation/list` 查询操作历史记录，支持按操作人、时间范围、操作类型等条件过滤， 本方法需要登录账户拥有至少以下一个 workspace 的权限: [toolbox -> auth-log -> operation-history]

修改接口 `/repo/list` 查询代码库列表， 本方法需要登录账户拥有至少以下一个 workspace 的权限: [system-config -> repositories，system-config -> analysis，toolbox -> large-screen]

```
 修改返回值

     新增字段 `creator: {"description":"创建者信息","allOf":[{"$ref":"#/components/schemas/Creator"}]}` 创建者信息

     修改字段 `id` 代码库ID

         类型从 `string(uuid)` 改为 `string`

     修改字段 `orgId` 所属组织id

         类型从 `string(uuid)` 改为 `string`

     修改字段 `status`

         类型从 `string` 改为 `EnumAnalysisStatus`

     修改字段 `readiness`

         类型从 `string` 改为 `EnumReadiness`

     修改字段 `latestAnalysisId` 最新一次成功分析的id

         类型从 `string(uuid)` 改为 `string`

     修改字段 `analysisId` 最新一次分析的id

         类型从 `string(uuid)` 改为 `string`

     修改字段 `languageDistribution` 代码库语言占比

         类型从 `{"type":"object","description":"代码库语言占比"}` 改为 `{"type":"object","description":"代码库语言占比","additionalProperties":{"type":"number"}}`
```

修改接口 `/project/list-repo` 查询项目中的代码库列表

```
 修改返回值

     新增字段 `creator: {"description":"创建者信息","allOf":[{"$ref":"#/components/schemas/Creator"}]}` 创建者信息

     修改字段 `id` 代码库ID

         类型从 `string(uuid)` 改为 `string`

     修改字段 `orgId` 所属组织id

         类型从 `string(uuid)` 改为 `string`

     修改字段 `status`

         类型从 `string` 改为 `EnumAnalysisStatus`

     修改字段 `readiness`

         类型从 `string` 改为 `EnumReadiness`

     修改字段 `latestAnalysisId` 最新一次成功分析的id

         类型从 `string(uuid)` 改为 `string`

     修改字段 `analysisId` 最新一次分析的id

         类型从 `string(uuid)` 改为 `string`

     修改字段 `languageDistribution` 代码库语言占比

         类型从 `{"type":"object","description":"代码库语言占比"}` 改为 `{"type":"object","description":"代码库语言占比","additionalProperties":{"type":"number"}}`
```

